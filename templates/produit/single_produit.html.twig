  
 <div class ="produit-item text-center">
    <a href ="{{ path('produit', {'slug':produit.slug })}}"><img src= "/uploads/{{produit.ullistration}}"alt ="{{ produit.nom }}" class="img-fluid"></a>
    <h5 class = "mt-3">{{ produit.nom }}</h5>
    <span class ="sousTitre"> {{ produit.sousTitre }} </span>
    <span class = "prix"> {{(produit.prix /100)| number_format(2) }}€</span>
</div>

    